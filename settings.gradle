pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        jcenter()
        mavenCentral()
        maven { url "https://jitpack.io" }
        maven {url "https://maven.google.com"}
        maven {
            url "https://gcp-in-jfrog.onedirect.in/artifactory/libs-release-local"
            credentials{
                username "onedirect-chat"
                password "onedirect-chat"
            }
        }
    }
}
dependencyResolutionManagement {
    // repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url "https://jitpack.io" }
        maven {url "https://maven.google.com"}
        maven {
            url "https://gcp-in-jfrog.onedirect.in/artifactory/libs-release-local"
            credentials{
                username "onedirect-chat"
                password "onedirect-chat"
            }
        }
    }
}

rootProject.name = "KukuFm_OneDirect"
include ':app'
